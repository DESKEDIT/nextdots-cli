#/bin/bash

PATHS="./nextdots ./nextdots.changeshelltheme ./nextdots.installdots"

if [[ -v DESTDIR ]]; then
  echo "$0: DESTDIR is set; skipping setting DESTDIR"
else
  DESTDIR="/bin"
fi

mkdir -p $DESTDIR
install $PATHS $DESTDIR
